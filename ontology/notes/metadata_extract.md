## Metadata to use
We will keep all the metadata associated with the files available, but few people will find it useful, so we'll 'expose' the selected ones.

### Open Calgary
k85e-i265_ResidentialSolarPhotovoltaicPvSystem_metadata.json
From metadata files:
"id", "name", "attribution", "createdAt", "description", "custom_fields", "metadata", "owner", "rights"

From feature files:
all available columns

### ENMAX
From metadata files:
"name", "description", "copyrightText", "editingInfo", "spatialReference"

From feature files:
all available columns