[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "calgary-dashboard"
version = "0.1.0"
description = "Calgary open data collection, cleaning, and dashboard tooling"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
]