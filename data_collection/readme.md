Each subdirectory in `/data_collection` contains scripts to download data (or access via API) from the respective source to the `/data` directory:

`/open_calgary` - Uses Socrata API, https://data.calgary.ca/
`enmax` - Uses Esri, https://www.enmax.com/system-resources
